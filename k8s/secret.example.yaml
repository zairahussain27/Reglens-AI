apiVersion: v1
kind: Secret
metadata:
  name: reglens-secrets
type: Opaque
stringData:
  GROQ_API_KEY: "YOUR_GROQ_API_KEY"
  # Optional: if your app reads it from env.
  # ALLOWED_ORIGINS is typically stored in ConfigMap.

